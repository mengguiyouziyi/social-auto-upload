# 开发环境 Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制 package 文件
COPY sau_frontend/package.json sau_frontend/package-lock.json ./sau_frontend/

# 安装依赖
RUN cd sau_frontend && npm ci

# 复制源代码
COPY sau_frontend ./sau_frontend

# 暴露端口
EXPOSE 5173

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]